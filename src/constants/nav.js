'use strict'

module.exports = {
  PERSIST: 'nav.persist',
  RESTORE: 'nav.restore',
  UPDATE: 'nav.update',
  SELECT: 'nav.select',

  PANEL: {
    TAB: {
      SELECT: 'ui.panel.tab.select'
    }
  }
}
