'use strict'

const TYPE = require('./type')

module.exports = {
  RESTORE: 'columns.restore',
  PositionColumn: {
    context: 'list',
    id: 'added',
    label: '',
    width: 40,
    type: TYPE.NUMBER
  }
}
