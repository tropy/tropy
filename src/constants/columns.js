'use strict'

const TYPE = require('./type')

module.exports = {
  RESTORE: 'columns.restore',
  PositionColumn: {
    context: 'list',
    id: 'added',
    label: '',
    width: 5,
    type: TYPE.NUMBER
  }
}
